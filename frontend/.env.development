# API base URL — leave empty in dev (Vite proxies /api to the backend)
VITE_API_BASE_URL=

# Vite dev server proxy target (Spring Boot)
VITE_API_PROXY_TARGET=http://localhost:8080

# Optional dev server port
VITE_DEV_PORT=5173
